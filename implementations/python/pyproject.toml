[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "aptx-python"
version = "0.1.0"
description = "Python implementation of the Aptos transaction CLI targeting aptos-python-sdk"

[tool.setuptools]
packages = ["aptx_py"]
